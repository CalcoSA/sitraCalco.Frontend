services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_CONFIGURATION: qa

    container_name: sitracalco-frontend-qa

    restart: unless-stopped

    ports:
      - "127.0.0.1:8081:80"